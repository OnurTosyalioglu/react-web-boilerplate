import React from 'react'
import { render } from '@testing-library/react'
import BottomInput from './BottomInput'

test('BottomInput component test', () => {
  const Render = render(<BottomInput />)

	expect(true).toBe(true)
})
