import React from 'react'
import { render } from '@testing-library/react'
import MessageTile from './MessageTile'

test('MessageTile component test', () => {
  const Render = render(<MessageTile />)

	expect(true).toBe(true)
})
