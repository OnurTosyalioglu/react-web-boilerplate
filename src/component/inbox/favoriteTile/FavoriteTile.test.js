import React from 'react'
import { render } from '@testing-library/react'
import FavoriteTile from './FavoriteTile'

test('FavoriteTile component test', () => {
  const Render = render(<FavoriteTile />)

	expect(true).toBe(true)
})
