import React from 'react'
import { render } from '@testing-library/react'
import LocaleSwitch from './LocaleSwitch'

test('LocaleSwitch component test', () => {
  const Render = render(<LocaleSwitch />)

	expect(true).toBe(true)
})
