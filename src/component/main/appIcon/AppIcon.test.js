import React from 'react'
import { render } from '@testing-library/react'
import AppIcon from './AppIcon'

test('AppIcon component test', () => {
  const Render = render(<AppIcon />)

	expect(true).toBe(true)
})
