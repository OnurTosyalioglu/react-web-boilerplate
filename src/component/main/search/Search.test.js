import React from 'react'
import { render } from '@testing-library/react'
import Search from './Search'

test('Search component test', () => {
  const Render = render(<Search />)

	expect(true).toBe(true)
})
