import React from 'react'
import { render } from '@testing-library/react'
import Footer from './Footer'

test('Footer component test', () => {
  const Render = render(<Footer />)

	expect(true).toBe(true)
})
