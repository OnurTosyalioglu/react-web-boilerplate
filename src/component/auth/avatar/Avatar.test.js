import React from 'react'
import { render } from '@testing-library/react'
import Avatar from './Avatar'

test('Avatar component test', () => {
  const Render = render(<Avatar />)

	expect(true).toBe(true)
})
