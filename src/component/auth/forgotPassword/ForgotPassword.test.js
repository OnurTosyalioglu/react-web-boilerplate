import React from 'react'
import { render } from '@testing-library/react'
import ForgotPassword from './ForgotPassword'

test('ForgotPassword component test', () => {
  const Render = render(<ForgotPassword />)

	expect(true).toBe(true)
})
