import React from 'react'
import { render } from '@testing-library/react'
import AccountIcon from './AccountIcon'

test('AccountIcon component test', () => {
  const Render = render(<AccountIcon />)

	expect(true).toBe(true)
})
