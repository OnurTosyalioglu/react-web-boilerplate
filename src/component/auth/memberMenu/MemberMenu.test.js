import React from 'react'
import { render } from '@testing-library/react'
import MemberMenu from './MemberMenu'

test('MemberMenu component test', () => {
  const Render = render(<MemberMenu />)

	expect(true).toBe(true)
})
