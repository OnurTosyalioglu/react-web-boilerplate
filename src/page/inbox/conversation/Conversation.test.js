import React from 'react'
import { render } from '@testing-library/react'
import Conversation from './Conversation'

test('Conversation component test', () => {
  const Render = render(<Conversation />)

	expect(true).toBe(true)
})
