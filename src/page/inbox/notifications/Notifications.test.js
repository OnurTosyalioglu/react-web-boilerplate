import React from 'react'
import { render } from '@testing-library/react'
import Notifications from './Notifications'

test('Notifications component test', () => {
  const Render = render(<Notifications />)

	expect(true).toBe(true)
})
