import React from 'react'
import { render } from '@testing-library/react'
import Favorites from './Favorites'

test('Favorites component test', () => {
  const Render = render(<Favorites />)

	expect(true).toBe(true)
})
