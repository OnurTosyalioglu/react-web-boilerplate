import React from 'react'
import { render } from '@testing-library/react'
import Inbox from './Inbox'

test('Inbox component test', () => {
  const Render = render(<Inbox />)

	expect(true).toBe(true)
})
