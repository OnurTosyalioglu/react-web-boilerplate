import React from 'react'
import { render } from '@testing-library/react'
import Recover from './Recover'

test('Recover component test', () => {
  const Render = render(<Recover />)

	expect(true).toBe(true)
})
