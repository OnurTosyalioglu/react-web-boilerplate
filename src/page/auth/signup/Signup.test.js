import React from 'react'
import { render } from '@testing-library/react'
import Signup from './Signup'

test('Signup component test', () => {
  const Render = render(<Signup />)

	expect(true).toBe(true)
})
